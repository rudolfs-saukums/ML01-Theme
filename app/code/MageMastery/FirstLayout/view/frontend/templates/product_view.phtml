<?php
/** @var \MageMastery\FirstLayout\ViewModel\ProductViewModel $viewModel */
$viewModel = $block->getData('view_model');
$product = $viewModel->getProductBySku('some-sku'); // Replace with actual SKU

if ($product) {
    echo '<h1>' . $product->getName() . '</h1>';
    echo '<p>' . $product->getPrice() . '</p>';
} else {
    echo '<p>Product not found.</p>';
}
?>
